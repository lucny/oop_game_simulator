A photorealistic 4K educational infographic divided into a 2x2 grid of four distinct 16:9 Full HD panels forming one cohesive comparison display of data flow and architecture in the tournament simulation system. Clean white background, subtle light grey separators, minimalist data center environment with data flow visualizations and storage systems. Unified modern sans-serif typography in Czech with correct grammar. Professional, highly readable, no cartoons, no emojis. Realistic data pipeline diagrams, JSON file representations, and object relationship visualizations. Consistent color logic across panels for data input, processing, storage, and output. Soft cinematic lighting, clean database and file system icons.
Top left panel: Vstupní data - players.json. Subheading: „Načtení seznamu hráčů ze souboru". Central scene shows JSON file structure displayed in modern code editor, with array of player objects each containing fields: nickname (string), gender (enum), state (country code). Semi-transparent overlay shows parsing workflow: načti soubor → parse JSON → validuj data → vytvoř Player objekty. Labels in Czech: „JSON formát", „13 hráčů", „Strukturovaná data", „Validace vstupů", „Polymorfní inicializace". Color logic: blue for JSON soubor, green pro Player objekty, orange pro proces parsování, grey pro metadata.
Top right panel: Tvorba turnaje - Architecture. Subheading: „Orchestrace objektů a jejich vztahy". Central scene shows architecture diagram: Main program → TournamentFactory → BaseTournament (with RoundRobinTournament/EliminationTournament) → Match → Player relationships. Shows objects with their attributes listed. Semi-transparent overlay indicates data flow: aplikace → factory → turnaj → zápasy → hráči. Labels in Czech: „Objektová hierarchie", „Kompozice a agregace", „Jednosměrné závislosti", „Čistá architektura", „Separace odpovědnosti". Color logic: purple pro abstraktní třídy, green pro konkrétní objekty, blue pro Player instance, orange pro Match instance.
Bottom left panel: Zpracování turnaje. Subheading: „Tok dat během turnaje - Round-robin příklad". Central scene shows flowchart of tournament execution: Play method → Generate schedule → For each round → For each match → Play match → Update standings. Data flows through match results collecting into detailed_results list. Semi-transparent overlay shows: programa play() → iterace kol → interakce hráčů → akumulace dat. Labels in Czech: „Iterativní zpracování", „Inkrementální aktualizace", „Stanovy v reálném čase", „Tabelární export", „Audit trail". Color logic: blue pro program flow, green pro data struktury, orange pro aktualizace, grey pro výsledky.
Bottom right panel: Výstupní data - tournament_results.json. Subheading: „Uložení detailních výsledků do JSON". Central scene shows comprehensive JSON output structure containing tournament_info (metadata), players array, winner object, matches array (each with detailed match data), final_standings table, and statistics summary. Shows visual representation of data hierarchy. Semi-transparent overlay indicates: rezultáty v paměti → serializace na JSON → zápis do souboru → archivace. Labels in Czech: „Komplexní struktura", „Hierarchická data", „Audit trail turnaje", „Reprodukovatelnost", „Analýza dat". Color logic: green pro tournament_info, blue pro players a matches, orange pro standings, purple pro statistiky.
Overall aesthetic: cohesive educational comparison layout demonstrating complete data flow architecture from input to processing to output, with visual emphasis on JSON data structures, object relationships, tournament execution flow, and result serialization, consistent spacing and typography, clear data pipeline diagrams with flow arrows, JSON code samples, object relationship maps, architectural diagrams with component boxes and connections, sharp anti-aliased graphics, high didactic clarity suitable for software architecture and data management education.
